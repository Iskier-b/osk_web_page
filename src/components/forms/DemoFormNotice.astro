---
import { cn } from "@/lib/utils";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<p
  class:list={cn(
    "border-border/80 bg-muted/50 text-muted-foreground rounded-md border px-3.5 py-3 text-sm leading-relaxed",
    className,
  )}
  role="note"
>
  Wersja demonstracyjna — wysyłanie formularza jest wyłączone.
</p>
